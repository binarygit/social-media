#flash
  %p= flash[:alert]

%h1 Login Form
= form_with scope: :session, url: '/login', method: 'post', data: { turbo: false, remote: true } do |f|

  = f.label :email
  = f.email_field :email

  = f.label :password
  = f.password_field :password

  = f.submit 'Login'
